/*
45. Use Array.from() to convert a string into an array of characters.
*/

let string = 'Samuelito';
let converted = Array.from(string);
console.log(converted);

let culiao = Array.from('culiao');
console.log(culiao);